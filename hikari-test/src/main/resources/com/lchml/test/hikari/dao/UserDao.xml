<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lchml.test.hikari.dao.UserDao">

    <select id="get" resultType="com.lchml.test.hikari.User">
        select * from user where id=#{id}
    </select>


    <!--auto generated Code-->
    <insert id="insertSelective" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="user.id!=null"> `id`,</if>
            <if test="user.name!=null"> `name`,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="user.id!=null">#{user.id},</if>
            <if test="user.name!=null">#{user.name},</if>
        </trim>
    </insert>

</mapper>

